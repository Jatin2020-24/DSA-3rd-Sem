#include <bits/stdc++.h>
using namespace std;

#define vi vector<int>
#define optimize()                \
    ios_base::sync_with_stdio(0); \
    cin.tie(0);                   \
    cout.tie(0)
#define pb push_back
#define f(i, x, n) for (int i = x; i < n; i++)
#define ll long long int

int main()
{
    optimize();

    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        int arr[n], number=0;
        string a;
        f(i,0,n){
            
        }
    }

    string bin_string = "10";
    
    number = stoi(bin_string, 0, 2);
    cout<<number;

    return 0;
}